<h1 class="bands"> All Bands</h1>

<div class="band-list">
    <ul class="band-list">
    <% Band.all.each do |band| %>
        <li><a href="<%=band_url(band)%>"><%= band.name %></a></li>
    <% end %>
    </ul>
</div>

<ber>
<ber>

<a href="<%= new_band_url%>">Add Band</a>


